import * as react_jsx_runtime from 'react/jsx-runtime';
import { U as UnicornSceneProps } from './types-BhyKhoTi.js';
export { a as UnicornPreset, b as UnicornStudioScene, c as UnicornVariableChangeCallback, d as UnicornVariableDefinition, e as UnicornVariableManifestEntry, f as UnicornVariableValue, g as UnicornVariables, h as UnicornVec2, i as UnicornVec3 } from './types-BhyKhoTi.js';

/**
 * React component for rendering Unicorn Studio WebGL animations.
 *
 * @remarks
 * This component wraps Unicorn Studio's WebGL animation system for use in React applications.
 * It handles SDK loading, scene initialization, placeholder display, and error
 * states. The bundled Unicorn Studio SDK is used by default, while `sdkUrl`
 * can override it with a custom script URL.
 *
 * The component requires either a `projectId` (to load from Unicorn Studio's servers)
 * or a `jsonFilePath` (to load from a local JSON file).
 *
 * @param props - The component props
 *
 * @example
 * Basic usage with project ID:
 * ```tsx
 * <UnicornScene
 *   projectId="your-project-id"
 *   width={800}
 *   height={600}
 * />
 * ```
 *
 * @example
 * With placeholder and callbacks:
 * ```tsx
 * <UnicornScene
 *   projectId="your-project-id"
 *   placeholder="/images/loading.png"
 *   onLoad={() => console.log("Scene loaded!")}
 *   onError={(err) => console.error(err)}
 * />
 * ```
 *
 * @example
 * Using local JSON file:
 * ```tsx
 * <UnicornScene
 *   jsonFilePath="/scenes/animation.json"
 *   scale={0.5}
 *   fps={30}
 * />
 * ```
 *
 * @example
 * Driving authored variables and a preset:
 * ```tsx
 * <UnicornScene
 *   projectId="your-project-id"
 *   preset="Dark Theme"
 *   variables={{ brandColor: "#7c3aed", intensity: 0.65 }}
 * />
 * ```
 */
declare function UnicornScene({ projectId, jsonFilePath, sdkUrl, width, height, scale, dpi, fps, altText, ariaLabel, className, lazyLoad, production, paused, variables, preset, onVariableChange, placeholder, placeholderClassName, showPlaceholderOnError, showPlaceholderWhileLoading, onLoad, onError, sceneRef, }: UnicornSceneProps): react_jsx_runtime.JSX.Element;

export { UnicornScene, UnicornSceneProps, UnicornScene as default };
